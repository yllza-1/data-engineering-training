# Verified Business Report – Day 7 SQL Detective Day

## Business Insights

### 1. Total Orders

**Insight:** Counted the total number of orders.

**Verified Result:** **14 total orders**

**SQL Query:**

```sql
SELECT COUNT(*) AS total_orders
FROM orders;
```

**Business Meaning:** The business has processed 14 orders in total.

---

### 2. Completed Orders

**Insight:** Counted completed orders.

**Verified Result:** **10 completed orders**

**SQL Query:**

```sql
SELECT COUNT(*) AS completed_orders
FROM orders
WHERE status = 'completed';
```

**Business Meaning:** Most orders have been successfully completed.

---

### 3. Pending Orders

**Insight:** Counted pending orders.

**Verified Result:** **2 pending orders**

**SQL Query:**

```sql
SELECT COUNT(*) AS pending_orders
FROM orders
WHERE status = 'pending';
```

**Business Meaning:** Two orders are still waiting to be completed.

---

### 4. Cancelled Orders

**Insight:** Counted cancelled orders.

**Verified Result:** **2 cancelled orders**

**SQL Query:**

```sql
SELECT COUNT(*) AS cancelled_orders
FROM orders
WHERE status = 'cancelled';
```

**Business Meaning:** Cancelled orders generated no revenue.

---

### 5. Total Customers

**Insight:** Counted registered customers.

**Verified Result:** **10 customers**

**SQL Query:**

```sql
SELECT COUNT(*) AS total_customers
FROM customers;
```

**Business Meaning:** The database contains 10 registered customers.

---

### 6. Total Products

**Insight:** Counted available products.

**Verified Result:** **8 products**

**SQL Query:**

```sql
SELECT COUNT(*) AS total_products
FROM products;
```

**Business Meaning:** The business currently offers 8 products.

---

### 7. Total Completed Revenue

**Insight:** Calculated total revenue from completed orders.

**Verified Result:** **1639**

**SQL Query:**

```sql
SELECT SUM(orders.quantity * products.price) AS completed_revenue
FROM orders
JOIN products
ON orders.product_id = products.product_id
WHERE orders.status = 'completed';
```

**Business Meaning:** Completed orders generated a total revenue of 1639.

---

### 8. Revenue by Product

**Insight:** Calculated revenue generated by each product.

**Verified Result:**

- Laptop – 700
- Monitor – 540
- Headphones – 150
- Desk Chair – 120
- Mouse – 105
- USB Cable – 24

**SQL Query:**

```sql
SELECT products.product_name,
       SUM(orders.quantity * products.price) AS revenue
FROM orders
JOIN products
ON orders.product_id = products.product_id
WHERE orders.status = 'completed'
GROUP BY products.product_name;
```

**Business Meaning:** Laptop generated the highest revenue, while USB Cable generated the lowest.

---

### 9. Revenue by Category

**Insight:** Calculated revenue generated by each category.

**Verified Result:**

- Electronics – 1240
- Accessories – 279
- Office – 120

**SQL Query:**

```sql
SELECT products.category,
       SUM(orders.quantity * products.price) AS revenue
FROM orders
JOIN products
ON orders.product_id = products.product_id
WHERE orders.status = 'completed'
GROUP BY products.category;
```

**Business Meaning:** Electronics is the highest-performing category.

---

### 10. Orders by City

**Insight:** Counted orders from each city.

**Verified Result:**

- Prishtina – 5 orders
- Vushtrri – 4 orders
- Mitrovica – 2 orders
- Ferizaj – 1 order
- Peja – 1 order
- Prizren – 1 order

**SQL Query:**

```sql
SELECT customers.city,
       COUNT(*) AS order_count
FROM orders
JOIN customers
ON orders.customer_id = customers.customer_id
GROUP BY customers.city;
```

**Business Meaning:** Prishtina has the highest customer demand.

---

# Validation Queries

### Validation 11 – Customers with More Than One Order

**Insight:** Identified customers who placed more than one order.

**Result:**

- Customer 1 → 2 orders
- Customer 2 → 2 orders
- Customer 3 → 2 orders
- Customer 4 → 2 orders

**SQL Query:**

```sql
SELECT customer_id,
       COUNT(*) AS order_count
FROM orders
GROUP BY customer_id
HAVING COUNT(*) > 1;
```

**Business Meaning:** These customers are repeat buyers and may represent loyal customers.

---

### Validation 12 – Top 3 Completed Orders by Total Amount

**Insight:** Found the three highest-value completed orders.

**Result:**

- Order 1 → 700
- Order 8 → 360
- Order 4 → 180

**SQL Query:**

```sql
SELECT orders.order_id,
       orders.quantity,
       products.price,
       orders.quantity * products.price AS total_amount
FROM orders
JOIN products
ON orders.product_id = products.product_id
WHERE orders.status = 'completed'
ORDER BY total_amount DESC
LIMIT 3;
```

**Business Meaning:** These orders generated the highest sales value.

---

### Validation 13 – Orders Not Completed

**Insight:** Listed all orders that are still pending or cancelled.

**Result:** 4 orders are pending or cancelled.

**SQL Query:**

```sql
SELECT *
FROM orders
WHERE status != 'completed';
```

**Business Meaning:** These orders have not generated revenue yet.

---

### Validation 14 – Highest Revenue Category

**Insight:** Identified the category that generated the most completed revenue.

**Result:** Electronics → 1240

**SQL Query:**

```sql
SELECT products.category,
       SUM(orders.quantity * products.price) AS revenue
FROM orders
JOIN products
ON orders.product_id = products.product_id
WHERE orders.status = 'completed'
GROUP BY products.category
ORDER BY revenue DESC
LIMIT 1;
```

**Business Meaning:** Electronics is the strongest-performing product category.

---

### Validation 15 – City with the Most Orders

**Insight:** Found the city with the highest number of customer orders.

**Result:** Prishtina → 5 orders

**SQL Query:**

```sql
SELECT customers.city,
       COUNT(*) AS order_count
FROM orders
JOIN customers
ON orders.customer_id = customers.customer_id
GROUP BY customers.city
ORDER BY order_count DESC
LIMIT 1;
```

**Business Meaning:** Prishtina is the city with the highest customer demand.
